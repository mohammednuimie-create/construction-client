# 🧪 دليل اختبار الاتصال

## اختبار الاتصال بين الفرونت إند والباك إند

### 1️⃣ اختبار من Terminal (Backend API)

```bash
node test-api-connection.js
```

هذا الاختبار يتحقق من:
- ✅ Health Check endpoint
- ✅ Get Suppliers endpoint  
- ✅ Get Payments endpoint

### 2️⃣ اختبار من المتصفح (Frontend)

افتح Developer Console في المتصفح (F12) وقم بتشغيل:

```javascript
import { testFrontendConnection } from './utils/testConnection';
testFrontendConnection();
```

أو استخدم في React Component:

```javascript
import { testFrontendConnection } from '../utils/testConnection';

// في useEffect أو عند النقر على زر
testFrontendConnection();
```

---

## 🔧 إذا واجهت مشاكل

### المشكلة: "Supplier.find is not a function"

**الحل السريع:**
1. **شغّل ملف الإعادة التشغيل**: انقر نقراً مزدوجاً على `إعادة-تشغيل-السيرفر.bat`
   
   **أو يدوياً:**
2. افتح Terminal في مجلد `server`
3. اضغط `Ctrl+C` لإيقاف السيرفر (إذا كان يعمل)
4. ثم شغّله: `npm run dev`

**ملاحظة:** هذا الخطأ يحدث لأن السيرفر يحتاج إلى إعادة تحميل نموذج Supplier الجديد

### المشكلة: "ECONNREFUSED"

**الحل:**
1. تأكد من أن السيرفر يعمل على `http://localhost:4000`
2. شغّل السيرفر: `cd server && npm run dev`

### المشكلة: CORS Error

**الحل:**
- تأكد من أن السيرفر يستخدم `cors()` middleware
- تحقق من `server/server.js`

---

## ✅ النتائج المتوقعة

عند نجاح الاختبار، يجب أن ترى:
- ✅ Health Check: `{"status":"healthy","database":"connected"}`
- ✅ Get Suppliers: قائمة الموردين (قد تكون فارغة `[]`)
- ✅ Get Payments: قائمة المدفوعات (قد تكون فارغة `[]`)

---

## 📝 ملاحظات

- إذا كانت القوائم فارغة، هذا طبيعي إذا لم تكن هناك بيانات في قاعدة البيانات
- يمكنك إنشاء بيانات تجريبية من خلال API أو من خلال واجهة المستخدم

