# 🔍 أين أجد إعدادات Build Settings في Render.com؟

## 📸 بعد اختيار Repository:

بعد أن تضغط على `construction-client` repository، ستظهر لك صفحة جديدة تحتوي على:

---

## 📋 الحقول التي ستراها:

### 1. **Name** (في الأعلى):
```
Name: construction-backend
```
- اكتب أي اسم تريده (مثل: `construction-backend`)

### 2. **Region** (قائمة منسدلة):
```
Region: [Singapore ▼]
```
- اختر أقرب منطقة (Singapore أو أي منطقة أخرى)

### 3. **Branch** (قائمة منسدلة):
```
Branch: [main ▼]
```
- اتركه `main` (أو `master` إذا كان branch الرئيسي)

---

## ⚙️ **Build & Deploy Settings** (القسم المهم):

هذا القسم موجود في **الجزء السفلي** من الصفحة. ابحث عن:

### **Root Directory:**
```
Root Directory: [server]
```
- **هنا اكتب:** `server`
- **مهم جداً!** هذا يخبر Render أن Backend موجود في مجلد `server`

### **Build Command:**
```
Build Command: [npm install]
```
- اتركه `npm install` أو اتركه فارغاً
- Render سيقوم بتشغيله تلقائياً

### **Start Command:**
```
Start Command: [npm start]
```
- اتركه `npm start`
- هذا سيشغّل `node server.js`

---

## 🔧 **Environment Variables** (المتغيرات):

### أين أجدها؟
1. **ابحث عن زر "Advanced"** في أسفل الصفحة
2. أو **ابحث عن قسم "Environment Variables"**
3. أو **قائمة منسدلة "Add Environment Variable"**

### ماذا أضيف؟
اضغط "Add Environment Variable" وأضف:

#### 1. MongoDB URI:
```
Key: MONGODB_URI
Value: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/construction-management?retryWrites=true&w=majority
```

#### 2. JWT Secret:
```
Key: JWT_SECRET
Value: your-super-secret-jwt-key-change-this
```

#### 3. Node Environment:
```
Key: NODE_ENV
Value: production
```

---

## 📍 **الترتيب في الصفحة:**

```
┌─────────────────────────────────────┐
│  Name: [construction-backend]       │  ← في الأعلى
│  Region: [Singapore ▼]              │
│  Branch: [main ▼]                   │
├─────────────────────────────────────┤
│                                     │
│  Build & Deploy Settings:          │  ← القسم المهم
│  ┌───────────────────────────────┐ │
│  │ Root Directory: [server]     │ │  ← هنا!
│  │ Build Command: [npm install]  │ │
│  │ Start Command: [npm start]     │ │
│  └───────────────────────────────┘ │
│                                     │
│  Environment Variables:             │  ← القسم المهم
│  ┌───────────────────────────────┐ │
│  │ + Add Environment Variable    │ │  ← اضغط هنا
│  └───────────────────────────────┘ │
│                                     │
│  [Advanced ▼]                      │  ← أو اضغط هنا
│                                     │
│  [Create Web Service]              │  ← في الأسفل
└─────────────────────────────────────┘
```

---

## 💡 **نصائح مهمة:**

### 1. إذا لم ترَ "Root Directory":
- **ابحث عن "Advanced"** واضغط عليه
- أو **قُم بالتمرير لأسفل** في الصفحة

### 2. إذا كانت الحقول معطلة:
- تأكد من أنك اختَرت Repository أولاً
- تأكد من أنك في صفحة "Configure" (الخطوة 2)

### 3. إذا لم تجد "Environment Variables":
- اضغط على **"Advanced"** أولاً
- أو ابحث عن **"Environment"** في القائمة

---

## 🎯 **الخطوات بالترتيب:**

1. ✅ **اختر Repository:** `construction-client`
2. ✅ **املأ Name:** `construction-backend`
3. ✅ **اختر Region:** `Singapore`
4. ✅ **Root Directory:** `server` ← **مهم جداً!**
5. ✅ **Build Command:** `npm install`
6. ✅ **Start Command:** `npm start`
7. ✅ **اضغط "Advanced"** → أضف Environment Variables
8. ✅ **اضغط "Create Web Service"**

---

## 📸 **ما الذي يجب أن تراه:**

بعد اختيار Repository، يجب أن ترى:
- صفحة جديدة تحتوي على حقول للإعداد
- قسم "Build & Deploy Settings"
- زر "Create Web Service" في الأسفل

---

## ❓ **إذا لم تجد الحقول:**

1. **تحقق من أنك في الصفحة الصحيحة:**
   - يجب أن ترى "2 Configure" في الأعلى (محدد باللون البنفسجي)

2. **تحقق من أنك اختَرت Repository:**
   - يجب أن ترى اسم Repository في الأعلى

3. **جرب التمرير لأسفل:**
   - بعض الحقول قد تكون في الأسفل

---

**إذا لم تجد الحقول، أرسل لي screenshot وأساعدك! 📸**

