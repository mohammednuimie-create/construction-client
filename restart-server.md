# 🔄 إعادة تشغيل السيرفر - حل مشكلة Supplier.find

## المشكلة:
```
Supplier.find is not a function
```

## الحل:

### الطريقة 1: إعادة تشغيل يدوي

1. **افتح Terminal في مجلد `server`**
2. **أوقف السيرفر** (إذا كان يعمل):
   - اضغط `Ctrl + C`
3. **شغّل السيرفر مرة أخرى**:
   ```bash
   npm run dev
   ```

### الطريقة 2: استخدام PowerShell/CMD

```powershell
# في مجلد المشروع الرئيسي
cd server
npm run dev
```

### الطريقة 3: إذا كان السيرفر يعمل في نافذة منفصلة

1. اذهب للنافذة التي يعمل فيها السيرفر
2. اضغط `Ctrl + C`
3. ثم شغّله مرة أخرى: `npm run dev`

---

## ✅ بعد إعادة التشغيل:

1. **تحقق من Console السيرفر** - يجب أن ترى:
   ```
   MongoDB connected
   Server running on port 4000
   ```

2. **اختبر الاتصال**:
   ```bash
   node test-connection.js
   ```

3. **افتح الفرونت إند** وافتح صفحة الموردين والسداد
4. **انقر على زر "🔄 تحديث البيانات"**

---

## 💡 ملاحظة:

إذا استمرت المشكلة بعد إعادة التشغيل:
- تأكد من أن ملف `server/models/Supplier.js` موجود
- تحقق من أن MongoDB يعمل
- افحص Console السيرفر للأخطاء



