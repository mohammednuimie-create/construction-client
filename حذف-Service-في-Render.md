# 🗑️ حذف Service في Render.com

## 📝 النص المطلوب:

في حقل التأكيد، اكتب بالضبط:

```
sudo delete web service construction-client
```

---

## ✅ خطوات الحذف:

1. **اكتب النص:** `sudo delete web service construction-client`
2. **تحقق من:** اسم Service يجب أن يطابق `construction-client`
3. **اضغط:** "Delete Web Service" (سيتم تفعيله بعد كتابة النص الصحيح)

---

## ⚠️ تحذير:

- ⛔ **هذا الحذف نهائي!**
- ⛔ Service سيتوقف فوراً
- ⛔ لا يمكن التراجع عن هذا الإجراء

---

## 🚀 بعد الحذف:

بعد حذف Service الحالي، يمكنك إنشاء Service جديد للـ Backend:

1. اضغط "+ New" → "Web Service"
2. اختر Repository: `construction-client`
3. Render سيقرأ `render.yaml` تلقائياً
4. أضف Environment Variables فقط

---

**اكتب النص واضغط Delete! 🗑️**


