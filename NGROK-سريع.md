# ⚡ Ngrok - المشاركة المؤقتة في 5 دقائق!

## 🎯 الحل الأسرع للمشاركة المؤقتة

---

## 📌 الخطوة 1: ثبت Ngrok

### الطريقة 1: تحميل مباشر
1. اذهب إلى: https://ngrok.com/download
2. حمل `ngrok.exe` للـ Windows
3. ضعه في أي مجلد (مثل: `C:\ngrok\`)

### الطريقة 2: عبر npm
```bash
npm install -g ngrok
```

---

## 📌 الخطوة 2: سجل حساب Ngrok (مجاني)

1. اذهب إلى: https://dashboard.ngrok.com/signup
2. سجل حساب
3. بعد تسجيل الدخول، ستجد **Auth Token**
4. انسخ الـ Token
5. في Terminal (PowerShell أو CMD):
   ```bash
   ngrok config add-authtoken YOUR_TOKEN_HERE
   ```

---

## 📌 الخطوة 3: شغل Backend

افتح Terminal في مجلد server:
```bash
cd C:\Users\MOHAMD\client\server
node server.js
```

اتركه يعمل! ✅

---

## 📌 الخطوة 4: شغل Ngrok

افتح Terminal جديد (اترك الأول يعمل):

```bash
ngrok http 4000
```

ستظهر لك شاشة مثل:
```
Forwarding   https://abc123.ngrok.io -> http://localhost:4000
```

**انسخ الـ URL** (مثل: `https://abc123.ngrok.io`)

---

## 📌 الخطوة 5: عدّل Frontend

افتح `src/utils/api.js` وعدّل السطر الأول:

```javascript
const API_BASE_URL = 'https://abc123.ngrok.io/api';
```

(استبدل `abc123.ngrok.io` بالـ URL الذي حصلت عليه)

---

## 📌 الخطوة 6: شغل Frontend

```bash
cd C:\Users\MOHAMD\client
npm start
```

---

## 📌 الخطوة 7: للمشاركة مع صديقك

### خيار 1: نفس الشبكة
- شارك: `http://localhost:3000`

### خيار 2: من أي مكان
افتح Terminal جديد:
```bash
ngrok http 3000
```

انسخ الـ URL (مثل: `https://xyz789.ngrok.io`)
**شارك هذا الرابط مع صديقك!** 🎉

---

## ✅ المميزات:

- ⚡ سريع جداً (5 دقائق)
- ✅ لا يحتاج GitHub
- ✅ لا يحتاج رفع ملفات
- ✅ مجاني 100%
- ✅ يعمل مباشرة من جهازك

---

## ⚠️ ملاحظات:

- Ngrok Free Plan يعطي URL عشوائي (يتغير كل مرة)
- Ngrok Free Plan محدود بـ 40 طلبات/دقيقة
- للمشاركة المؤقتة - مثالي! ✅

---

## 🎯 ملخص سريع:

1. ثبت Ngrok
2. سجل وخذ Auth Token
3. شغل Backend (`node server.js`)
4. شغل Ngrok (`ngrok http 4000`)
5. عدّل `src/utils/api.js` بالـ URL
6. شغل Frontend (`npm start`)
7. للمشاركة: استخدم `localhost:3000` أو `ngrok http 3000`

---

**جاهز! ابدأ الآن 🚀**


